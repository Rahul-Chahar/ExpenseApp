<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Edit Expense</title>
</head>
<body class="bg-gray-100 p-6">
  <div class="container mx-auto">
    <h1 class="text-2xl font-bold mb-4">Edit Expense</h1>
    <form action="/expenses/update/<%= expense.id %>" method="POST" class="mb-6">
      <div class="mb-4">
        <input type="number" name="amount" value="<%= expense.amount %>" class="border p-2 w-full" required>
      </div>
      <div class="mb-4">
        <input type="text" name="description" value="<%= expense.description %>" class="border p-2 w-full" required>
      </div>
      <div class="mb-4">
        <select name="category" class="border p-2 w-full" required>
          <option value="food" <%= expense.category === 'food' ? 'selected' : '' %>>Food</option>
          <option value="transport" <%= expense.category === 'transport' ? 'selected' : '' %>>Transport</option>
          <option value="entertainment" <%= expense.category === 'entertainment' ? 'selected' : '' %>>Entertainment</option>
          <option value="health" <%= expense.category === 'health' ? 'selected' : '' %>>Health</option>
          <option value="education" <%= expense.category === 'education' ? 'selected' : '' %>>Education</option>
        </select>
      </div>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2">Update Expense</button>
    </form>
  </div>
</body>
</html>
